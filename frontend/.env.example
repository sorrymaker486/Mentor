# 可选：API 根路径。留空则使用默认 /api（需配置 Vite 代理或 Nginx 转发）。
# VITE_API_BASE=http://127.0.0.1:8001
# 开发时代理目标（仅 vite.config 读取，非 Vite 内置变量前缀也可在 config 里用 loadEnv 第三个参数 '' 读到）
# VITE_PROXY_TARGET=http://127.0.0.1:8001
